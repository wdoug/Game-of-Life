/*
* File for various configuration parameters for the app.
*
* If templating is added to this app, this file could be used for
* other things that could possibly change such as site title, or even
* potentially used for internationalization of any ui component.
*/
'use strict';

var WORLD = WORLD || {};

WORLD.config = {
    defaults: {
        boardSize: 20
    }
};
